name = "health-md-pricing-analytics"
main = "src/index.ts"
compatibility_date = "2026-05-16"

# Create once with:
#   wrangler d1 create health-md-pricing-analytics
# Then replace database_id below with the returned UUID before deploying.
[[d1_databases]]
binding = "DB"
database_name = "health-md-pricing-analytics"
database_id = "REPLACE_WITH_D1_DATABASE_ID"

[vars]
# This worker deliberately does not store IP addresses, user agents, request URLs,
# health values, metric names, vault/file paths, or exported content.
MAX_BATCH_SIZE = "50"

# Optional but recommended for deployed ingestion. Configure with:
#   wrangler secret put INGEST_TOKEN
# If unset, local/dev requests are accepted without Authorization.
